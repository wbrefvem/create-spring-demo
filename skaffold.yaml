apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/wbrefvem/create-spring-demo:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/wbrefvem/create-spring-demo:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: create-spring-demo
        chartPath: charts/create-spring-demo
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/wbrefvem/create-spring-demo"
          image.tag: "{{.DIGEST_HEX}}"
